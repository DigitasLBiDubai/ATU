@model List<string>

    <thead>
        <tr>
            @foreach (var title in Model)
            {
                <th>
                    <div>@title</div>
                </th>
            }
        </tr>
    </thead>